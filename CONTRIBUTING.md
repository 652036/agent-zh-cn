# 贡献指南

agent-zh-cn 接受译文修正、漏翻补充、安装器修复和软件适配改进。提交前请搜索已有 Issue，确认问题和改动范围。

## 报告问题

缺陷报告应包含目标软件及版本、操作系统、安装方式、复现步骤、预期行为和实际结果。报告漏翻时，请提供界面位置和完整原文，保留大小写、标点和变量；必要时附脱敏截图。

请移除截图和日志中的账户信息、凭据、个人路径及项目内容。安全漏洞按 [安全政策](SECURITY.md) 私下报告。

## 开发环境与项目结构

开发需要 Python 3.10+ 和 Node.js。Windows 原生安装器的验证需要 Windows PowerShell 5.1。

| 文件 | 用途 |
| --- | --- |
| `agent_zh.py` | 多软件安装、移除和状态检查 |
| `AgentZh.ps1` | Windows 入口，以及无 Python 依赖的原生实现 |
| `src/apps.json` | 软件身份、安装路径和命令行入口适配 |
| `src/runtime.js` | 界面翻译、动态节点处理和用户内容保护 |
| `locales/zh-CN.json` | Cursor 补充词典 |
| `locales/common.json` | 共用界面词典和动态文案规则 |
| `locales/devin-nls.json` | Devin 本地化语言资源的补充译文 |
| `scripts/build_js.py` | 合并界面词典并生成运行时脚本 |
| `scripts/scan_app_strings.py` | 从已安装软件中提取候选界面文案 |
| `agent-zh.js`、`cursor-zh.js` | 生成的运行时脚本和 Cursor 兼容副本 |
| `tests/` | 安装器样本、回滚、配置保留和运行时保护测试 |

`locales/devin-nls.json` 由安装器用于补充语言资源，不直接打包到 DOM 运行时词典。不要直接编辑 `agent-zh.js` 或 `cursor-zh.js`。

## 修改翻译

1. 在目标软件中确认文案位置及完整原文。
2. 根据来源修改 Cursor 词典、共用词典或 Devin 语言资源词典。
3. 修改 DOM 词典或运行时源码后，运行 `python scripts/build_js.py`，同步两个生成文件。
4. 执行相关检查，并在目标软件中验证译文和相邻的用户内容。

DOM 词典分为三个字段：

- `phrase`：完整句子或较长的界面标签。
- `short`：按钮、菜单等控件中的短标签，匹配范围比普通文本小。
- `patterns`：包含变量的提示，使用 JavaScript 正则表达式和 `$1` 等捕获组替换语法。

`locales/common.json` 的同名条目会覆盖 Cursor 词典中的条目。合并后同一键只保留在一个文本字段中。正则应锚定预期提示，不能扩展为任意文本替换。

译文使用简体中文，并沿用软件已有的术语。保留产品名、模型名、命令 ID、路径、URL、快捷键和插值参数，例如 `{0}`、`$1`。同一 JSON 对象中不得重复定义键。

只翻译界面文案。代码、用户输入、文件名、会话名称、聊天正文、协议字段和内部调试文本不应加入词典。调整保护规则时，需要同时验证应当翻译的控件和不应翻译的内容。

候选文案可通过以下命令扫描：

```sh
python scripts/scan_app_strings.py --app devin --output missing.json
```

扫描结果需要人工筛选，不能直接合并。不要提交本机扫描结果、配置、备份或日志。

## 修改安装器与添加软件适配

适配器必须定义明确的软件身份及有限的已知窗口入口。新布局、便携配置、活动版本子目录、额外会话窗口和专有语言资源，应分别提供对应的测试样本。

应用资源和用户配置的写入应纳入故障回滚；备份应区分软件身份、路径、版本、缺失文件和空文件。移除按当前资源处理，避免将升级后的应用替换为历史备份。

进程操作应限定为用户选择的软件。Windows 还需区分桌面应用与同名命令行程序，不得使用完整命令行模糊匹配来批量结束进程。

Python 和 PowerShell 原生实现应保持基本操作一致。新增仅适用于某一入口的选项时，应在文档中明确说明。

## 验证与提交

修改词典或运行时后先生成文件：

```sh
python scripts/build_js.py
```

代码和词典改动应执行以下检查：

```sh
python scripts/build_js.py --check
python -m unittest discover -s tests -v
node --check agent-zh.js
node tests/runtime.test.cjs
git diff --check
```

Windows 还需检查两个 PowerShell 入口的 5.1 语法兼容性及原生安装器样本。纯文档改动核对命令、链接和格式即可。

CI 使用临时安装样本检查资源写入和回滚，运行时测试检查界面文本和用户内容的处理。实际软件的界面验证应另行记录操作系统、软件版本和检查的页面，不能用 CI 结果代替。

Pull Request 应说明解决的问题、修改后的行为、受影响的软件和验证结果。提交源文件及需要更新的两个生成文件，排除本机构建产物和无关改动。公开提交中还应检查作者邮箱；需要隐藏个人邮箱时使用 GitHub 提供的匿名邮箱。
