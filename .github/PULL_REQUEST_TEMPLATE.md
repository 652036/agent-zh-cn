## 改动说明

说明本次改动解决的问题和实现方式。

## 验证

说明执行的检查及结果。纯文档改动核对命令、链接和格式即可；不适用的项目请注明原因。

- [ ] 已运行 `python scripts/build_js.py --check`（如适用）
- [ ] 已运行 `python -m unittest discover -s tests -v`（如适用）
- [ ] 已运行 `node --check agent-zh.js` 和 `node tests/runtime.test.cjs`（如适用）
- [ ] 已在相关操作系统和目标软件版本中验证（如适用）

## 提交确认

- [ ] 改动只包含与本 Pull Request 有关的内容
- [ ] 自动生成的 `agent-zh.js` 已同步更新（如适用）
- [ ] 日志、截图和提交内容不包含敏感信息
