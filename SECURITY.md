# 安全政策

安全修复面向 agent-zh-cn 的当前维护版本。报告问题时请提供项目版本或提交号，以及目标软件、操作系统和安装方式。

## 报告安全问题

请通过 [GitHub Security Advisories](https://github.com/652036/agent-zh-cn/security/advisories/new) 私下报告漏洞，并附最小复现步骤、实际影响和脱敏后的必要材料。

如果私密报告入口不可用，可在普通 Issue 中请求维护者提供联系渠道，暂不附漏洞细节。请勿公开令牌、Cookie、账户信息、个人路径或完整日志。

## 文件与配置修改范围

安装器会处理以下资源：

- 已识别的应用工作台、会话窗口 HTML，以及相邻的 `agent-zh.js`。
- `product.json` 中与这些窗口入口对应的已有校验值。
- 目标软件的 `argv.json`、`User/locale.json` 和语言包索引。
- 用户数据目录下的 `AgentZh/main.i18n.json` 补充语言资源。
- `AgentZh/backup` 下的原文件快照和清单。

为清理由早期 CursorZh 版本留下的安装痕迹，安装器仍会识别并移除旧 `cursor-zh.js` 注入及文件；仓库不再发布该旧运行时。正常移除后保留官方中文语言包和中文语言设置。

备份清单含原始路径，备份文件可能包含用户配置。它们仅用于本地恢复，不应随源码、日志或问题报告公开上传。

## 网络与进程操作

安装器可调用目标软件自带的命令行工具下载微软简体中文语言包。`--no-langpack` 或 PowerShell 的 `-NoLangpack` 可跳过下载。补充翻译脚本本身不发起网络请求，也不上传界面内容。

`--kill`、`-Kill` 和一键批处理允许关闭所选软件，运行前应保存工作。`--restart` 或 `-Restart` 会在操作完成后启动软件。Python 的 `--defer-restart` 跳过进程操作和语言包下载，但仍会写入应用资源及语言配置。

本项目不索取软件登录凭据，也不修改账户、订阅、模型请求或用量记录。

## 恢复与限制

安装器在写入前保存备份，并尝试回滚失败操作中已完成的文件写入。磁盘或权限故障可能使回滚无法完成；遇到相关错误时，应保留备份和错误信息，停止重复安装。

修改应用资源可能影响厂商的代码签名和自动更新。对无法识别的软件身份或入口，安装器应停止处理。发现越界写入、错误的进程操作或其他超出本文范围的行为时，请提交安全报告。
